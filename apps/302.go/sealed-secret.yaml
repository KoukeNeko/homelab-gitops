# Placeholder. Run ./seal.sh to replace this with a real SealedSecret that
# carries ADMIN_PASSWORD_HASH and API_TOKEN. Until then the pod waits in
# CreateContainerConfigError, which is the intended fail-safe.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: 302go-secrets
  namespace: 302go
spec:
  encryptedData:
    ADMIN_PASSWORD_HASH: REPLACE_WITH_SEALED_VALUE
    API_TOKEN: REPLACE_WITH_SEALED_VALUE
  template:
    metadata:
      name: 302go-secrets
      namespace: 302go
